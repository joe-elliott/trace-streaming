receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  native:
  native/custom:
    blockDuration: 5m

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [native]