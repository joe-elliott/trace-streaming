receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  native:
  native/custom:
    block_duration: 5m

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [native]